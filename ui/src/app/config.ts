export const AUTH_STORAGE_KEY = "decisionEngineAdminToken";
export const DEFAULT_PAGE_SIZE = 5;

/** Dev-only UI chrome (banner, local token instructions). False in production builds. */
export const SHOW_DEV_DEMO_UI = import.meta.env.DEV;
